int fiba(int n) {
    return fiba(n- 1) + fiba(n - 2);
};

int main() {
    int num, ans, i;
    in num;

    for (i = 0; i < num; ++i) {
        ans = fiba(num);

        out "Your Fibonacci number is: ";
        out ans;
    }

    return 0;
}