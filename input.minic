int fiba(int n) {
    if (n == 0 || n == 1) return 1;
    return fiba(n - 1) + fiba(n - 2);
}

int main() {
    int num, res, i;
    in num;

    for (i = 0; i < num; ++i) {
        res = fiba(i);

        out "Awesome number = ";
        out res;
    }

    return 0;
}